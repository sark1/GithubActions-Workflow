name: test1
on:
  push:
jobs:
  get_artifact:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: 'powershell'
        shell: pwsh
        run: |
             'Install-Module powershell-yaml -force'
             'Import-Module powershell-yaml -Force'
             'invoke-webrequest -Uri "https://github.com/sark1/GithubActions-Workflow/blob/main/Dockerfile" -OutFile "abcd.txt"'
             'Write-Host "Artifact Verified !!!"
        
